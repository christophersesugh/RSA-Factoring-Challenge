#include "factors.h"

/**
 * main - main function
 *
 *
 * Return: 0 (Success)
 */
int main(int argc, char **argv)
{
	FILE *fp;
	size_t count;
	ssize_t line;
	char *buffer = NULL;

	if (argc < 2)
	{
		fprintf(stderr, "USAGE: factors <file>");
		exit(EXIT_FAILURE);
	}
	return (0);
}

